language: python
python:
  - '3.7'

install:
  - pip install pipenv
  - pipenv install

script:
    - pipienv run travis-sphinx build --source docs

after_success:
    - pipienv run travis-sphinx deploy
